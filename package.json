{
  "name": "ahkstrategies-project",
  "version": "1.0.0",
  "description": "AHKstrategies static site + server + cypress tests",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:server": "node server.js",
    "start:detached": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/runDetached.ps1",
    "test": "cypress run --config baseUrl=http://localhost:8080",
  "screenshot:hero": "cypress run --spec cypress/e2e/hero_screenshot.cy.js --config baseUrl=http://localhost:8080 && node ./scripts/save_hero_artifact.js",
    "test:open": "cypress open --config baseUrl=http://localhost:8080",
    "e2e": "node ./scripts/e2e.js",
    "ci": "npm ci"
  },
  "keywords": [],
  "author": "auto-generated",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "cypress": "^12.17.0",
    "cypress-axe": "^1.4.0"
  }
}
